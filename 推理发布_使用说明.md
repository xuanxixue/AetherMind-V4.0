# AetherMind V4 推理发布包 · 使用说明

## 一、这是什么

AetherMind V4 是一个物理机制增强的小型对话模型（约 1 亿参数，d_model=384，6 层，
上下文 256 token），基于 Qwen2.5-0.5B 的分词器，在中文对话语料上训练至 100000 步
（Phase G）。本发布包为**推理运行包**，不含训练代码与训练数据。

对话能力由两部分组成：

1. **模型生成**：物理注意力（信息素热力学）+ RAG 检索增强；
2. **检索直答（默认开启）**：内置 3700+ 条参考对话与知识图谱，高置信命中时直接返回
   参考答案，适合日常问答；输入 `/rag` 可关闭，强制使用模型原生生成。

## 二、环境要求

- Windows 10/11（64 位），或 Linux/macOS（命令行方式运行）
- Python 3.10 ~ 3.12
- NVIDIA 显卡推荐（4GB 以上显存即可，实测峰值约 1.3GB）；无显卡可自动回退 CPU
  （速度较慢）
- 离线可用：分词器与全部数据均已打包，无需联网

## 三、安装（仅首次）

在本文件夹内打开命令行，执行：

```
pip install -r requirements.txt
```

如有 NVIDIA 显卡，建议安装 CUDA 版 PyTorch（见 https://pytorch.org ），
CPU 版 torch 也可直接运行。

## 四、启动

**方式一（推荐）**：双击 `run_inference.cmd`，进入交互式对话。

**方式二（命令行单次生成）**：

```
python scripts\inference_v4.py --prompt "你想问的话" --max_new 200
```

程序会自动加载 `checkpoints_v4_fixed\` 下最新的 checkpoint，无需手动指定。

## 五、交互命令

| 命令 | 作用 |
|------|------|
| 直接输入文字 | 对话 |
| `/learn` | 开/关在线演化（信息素沉积与固化，部署后持续学习） |
| `/stats` | 查看信息素浓度、固化质量与固化轮数 |
| `/consolidate` | 手动触发一次 LTP 固化 |
| `/temp 0.8` | 设置采样温度（越低越稳定） |
| `/max 200` | 设置最大生成长度 |
| `/kg` | 开关知识图谱检索增强 |
| `/ref` | 开关对话语料检索增强 |
| `/rag` | 开关检索直答（关闭后全部走模型生成） |
| `/memory` | 查看对话记忆状态 |
| `/reset` | 清空对话记忆与信息素（长期固化保留） |
| `/exit` 或空行 | 退出 |

## 六、目录结构

```
AetherMind-V4-Inference/
├── run_inference.cmd              一键启动（交互对话）
├── requirements.txt               Python 依赖
├── 使用说明.md                    本文件
├── scripts/
│   ├── inference_v4.py            推理主程序
│   └── convert_train_to_inference.py  训练权重→推理架构转换（可选进阶）
├── src/                           模型源码（物理注意力/双域/记忆等模块）
├── configs/                       模型配置
├── models_local/Qwen/...          Qwen2.5 分词器（离线）
├── checkpoints_v4_fixed/
│   └── v4_checkpoint_100000_phaseG_final.pt   训练权重（约 1.1GB）
├── 03_dialogue/                   检索数据（知识图谱 + 参考对话）
├── docs/                          架构技术文档
└── AetherMind监控中心.exe          训练监控工具（训练用，推理非必需）
```

## 七、已知说明

- 本模型为 1 亿参数小模型，**原生生成文本可能不够通顺**，这是模型规模决定的能力
  上限；日常使用建议保持检索直答开启（默认）。
- 生成已在对话回合结束标记（`<eom>`）处正确停止，不会自演多轮假对话。
- 训练架构 checkpoint 中的绝对坐标信息素 τ 不跨会话迁移，加载时会自动清空
  （位置绑定偏置在新会话中是噪声）；如需保留训练出的相对信息素，可运行：

```
python scripts\convert_train_to_inference.py
```

  生成 `*_inference.pt` 推理架构权重，推理程序会自动优先加载它。

## 八、发布包内容与开发目录的关系

本包由 `build_release.cmd` 从开发目录自动复制生成，**未删除开发目录中的任何文件**。
训练数据（约 13GB）、旧 checkpoint、训练脚本、node_modules 等开发产物未包含在本
发布包内，以控制体积。
